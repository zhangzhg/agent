# 《太一仙途》游戏设计详细文档

本文档是 [README.md](README.md) 第二部分（游戏设计）的展开：把"无主线、随机接龙"的设计理念落成可执行的内容规格、数值表、界面交互与内容生产计划。目标读者是内容策划、前端与关卡人员。

**三份文档的关系**：README 定"要做什么、为什么这样设计"；[ARCHITECTURE.md](ARCHITECTURE.md) 定"底座怎么跑"（`GameEventDef`/`BalanceTable`/`PlayTurnService` 等数据结构与流程）；本文档定"内容长什么样、界面怎么摆、数值怎么配"——三者共用同一套术语，互为参照，不重复定义规则本身。

---

## 1. 体验目标

**一句话定位**：没有教程、没有任务列表的文字修仙沙盒——玩家靠脑洞打字，系统靠随机接龙给意外。

**核心情感曲线不是"变强曲线"，是"意外感曲线"**。传统 RPG 靠数值增长和任务完成度制造成就感；本作没有任务日志可勾，成就感来自"我这句话又抽出了什么"。这决定了三条硬性设计原则：

1. **永远不让玩家觉得"抽到了标准答案"**——同一输入在不同局应该有不同下文（1.4.3 新鲜感机制的产品目标）。
2. **失败也要有内容，不能是空白**——"条件未满足"要说出**为什么**（没带玉佩/年龄不够），玩家才能调整下一句，而不是对着空气瞎猜。
3. **不用进度条制造焦虑**——境界、寿元这类数值展示要克制（见 §2.4），本作的驱动力是好奇不是恐慌。

**目标玩家画像**：喜欢文字 MUD/互动小说、对"被剧本安排"感到疲劳、愿意用打字而非点击菜单交互的玩家。不是重度数值养成玩家的主场（数值只是骨架，不是卖点）。

### 1.1 首次上手（无教程设计）

不做强制教程，但**开局叙述本身要暗示可做什么**，靠系统消息里嵌自然语言提示，而不是弹窗步骤条：

```
[系统] 太乙历一百年，你生于苍梧城，是个六岁的孩童。
       街角传来阵阵饭香，酒楼里似乎有人在吆喝什么。
       （试着打字告诉我你想做什么，比如"去酒楼看看"）
```

- **提示只出现在前 3 轮对话**，之后不再赘述括号提示，避免变成事实上的教程文本。
- 玩家输入完全不在别名表里时（`ChatParser.parse` 返回 `None`），系统回复带一个**软性引导**而非单纯"听不懂"：「听不懂，要不试试"去XX"或"吃饭"？」——从当前地点的合格事件池里现取一两个 `event_id` 的常见别名做例子，不是写死的固定文案。
- 前 10 分钟目标：让玩家自然触发一次命令事件（如「吃饭」）+ 一次奇遇（哪怕是"无事发生"），体会到"我说的话真的改变了什么"。

---

## 2. 界面与交互设计

单机文字游戏，**聊天框是唯一操作入口**，其余面板只读展示，不放可点击的技能栏/任务栏（对应 README"不是技能栏或任务面板"的产品边界）。

### 2.1 整体布局

```
┌─────────────────────────────┬───────────────┐
│                               │  太乙历九四七六年 │
│                               │  甲辰年 三月十五  │
│         叙述滚动区             │  午时  ☾潮汐日    │
│   （叙述文本 + 系统提示）       ├───────────────┤
│                               │ 苍梧城 · 酒楼     │
│                               │ 灵气：●●●○○     │
│                               ├───────────────┤
│                               │ 练气三层 68/100  │
│                               │ 饱食 ●●●●○      │
│                               │ 心境 平和         │
│                               ├───────────────┤
├───────────────────────────────┤ 金钱 32          │
│ > 输入你想做什么…               │ 背包 [3]         │
└───────────────────────────────┴───────────────┘
```

- **左侧叙述滚动区**：只有两种气泡样式——叙述文本（无边框、正常字重）与系统提示（灰底、斜体，如"听不懂""条件未满足"），二者视觉上明确区分，玩家一眼能分清"世界发生了什么"和"系统在跟我说话"。
- **右侧状态栏四段**：历法牌（§2.3）/ 位置与灵气 / 角色属性 / 金钱与背包，全部只读，点击无操作，纯信息面板。
- **底部输入框**：单行文本框，无快捷指令按钮、无自动补全下拉（避免变相技能栏）；仅在 `EncounterPendingState`/`ScenarioPendingState` 下，输入框上方浮现 1~3 个**灰色小字建议**（取自 `ReplyOption.aliases` 或当前流程图出边的示例短语），点击可代填但不代发送，玩家仍需确认或改写后回车——建议永远是"提示"不是"选项按钮"，维持"打字而非点选"的体验基调。

### 2.2 挂起提示的呈现（对应 ARCHITECTURE `EncounterPendingState`）

奇遇挂起时，叙述区末尾追加一行淡色小字，不打断叙述本身的沉浸感：

```
有人钓上一条金龙鱼，摊主吆喝着要卖。
                                  （你打算怎么办？）
```

不列举选项，因为 README 的立场是"聊天里下一句继续脑洞"，列选项等于变相任务面板。`ReplyOption` 的存在是给**解析兜底**，不是给**界面展示**——这是本设计与常见 Galgame 分支 UI 的关键区别。

### 2.3 万年历面板（对应 2.3.1）

- **历法牌**：`太乙历 九四七六年 甲辰年 三月十五 午时`，来自 `view/calendar_view.render_calendar_plaque`。
- **罗盘**：十二时辰刻度，指针随 `GameTime.shichen` 转动；潮汐日（初一/十五）罗盘边缘亮青色光晕；天劫窗口期（境界已到 + 日历推算命中窗口）边缘转暗红——**只是氛围提示，不是倒计时**，不显示"还剩几天可能天劫"的精确数字，保留不确定感。
- **点击历法牌**：展开近期"游戏大事"摘要（最近 5 条 `CosmicEvent`/`MapUpdateEvent`），只读。

### 2.4 角色状态面板（数值展示的克制原则）

- **境界**：显示当前境界 + 修为进度条（如"练气三层 68/100"），条到头触发突破**不自动**，仍需玩家喊"突破"。
- **寿元**：**不显示具体数字**，只显示模糊态（"年富力强" / "略感体力不济" / "大限将近"），避免变成焦虑倒计时——这是刻意的反数值化设计，寿元只在"闭关结算摘要"和临终前才露出具体数。
- **心魔/悟性等隐藏属性**：完全不在面板展示，只通过叙述文本侧面体现（心魔高时叙述会插入"你隐隐感到一丝烦躁"），保留"角色状态需要靠观察叙述来感知"的沉浸感。
- **饱食**：五格图标，纯装饰化的生存反馈，不做成"饿死倒计时"式的红字警告。

### 2.5 位置/地图面板

- 当前地点名 + 地点类型 + 灵气浓度五格条。
- **不放"可去地点"跳转列表**——移动同样靠聊天（「去酒楼」），面板只做只读展示，维持单一输入入口。
- 神识扫描等主动探索技能触发后，面板短暂高亮新发现的地点属性变化（如危险等级上升），3 回合后恢复常态。

### 2.6 背包面板

- 简单堆叠列表：图标 + 名称 + 数量，点击查看物品描述（只读 tooltip）。
- 使用/丢弃物品仍靠聊天（「吃丹药」「把玉佩给他」），面板不放"使用"按钮。

---

## 3. 核心玩法内容规格

### 3.1 命令语言与别名表设计

`ChatParser` 的别名表是内容策划的日常工作量大头。给出设计规范与示例：

| event_id | 类型 | 别名示例（`GameEventDef.aliases`） | 说明 |
|---|---|---|---|
| `move` | 系统命令（非库内事件，直接映射） | 「去{地点}」「前往{地点}」「回{地点}」 | 地点名走模糊匹配，不要求精确 |
| `eat` | 命令 | 「吃饭」「去吃点东西」「果腹」「填饱肚子」 | 至少 4 条别名起 |
| `meditate` | 命令 | 「打坐」「运功」「修炼」 | |
| `breakthrough` | 命令 | 「突破」「冲击境界」「渡劫」（境界到大乘时复用） | |
| `watch` | 命令 | 「围观」「看看」「凑过去看」 | 需要 `scene_focus` 不为空才生效，否则回退"看看四周"→触发地点通用描述，不进合格池 |
| `buy` | 命令 | 「买下来」「把{物品}买下」「付钱」 | 挂起态优先走 `ReplyOption`，全局兜底走这条 |
| `apprentice` | 命令 | 「拜师」「请求加入」「求收留」 | |
| `retreat_start` | 系统命令（触发 `RetreatService`） | 「闭关」「闭关修炼」 | 触发后追加系统询问"闭关多久？" |
| `inspect_npc` | 系统命令（只读查询，不进事件总线） | 「看看那人」「打听{NPC}」 | 返回信息卡，不改状态、不计入 `AgentEventHistory` |
| `flee` | 命令 | 「逃」「跑」「撤」 | 战斗/危险检定分支专用 |
| `fight` | 命令 | 「动手」「打他」「出手」 | |

**代词解析（"它"/"这个"）**：`ChatParser.parse` 在命中"买/用/看"等及物动词但缺宾语时，用 `agent.scene_focus` 回填宾语——「把它买下来」等价于「把{scene_focus}买下来」。`scene_focus` 由上一条 `AppliedDiff.scene_focus_set` 写入，一步过期不清空，允许多轮指代（"围观"→"买下来"→"再看看它"）。

**只读查询命令（`inspect_*`）单独设计**：不是所有输入都要走两段式 `GameEventOccurrence`。查询类命令（看NPC信息卡、查看历法详情、查看背包物品描述）直接由 `controller` 调只读服务返回，**不进 `PlayTurnService`**，因为它们不改变游戏状态、不该消耗时间、不该触发第二段抽取。

### 3.2 标签体系

| 标签 | 含义 | 典型 event | 粗筛配额优先级（对应 ARCHITECTURE 1.4.3 标签配额） |
|---|---|---|---|
| `生活` | 吃饭、闲逛、购物 | eat / stroll | 基线，权重不特别加成 |
| `修炼` | 打坐、突破、心得 | meditate / breakthrough | 与「生活」交替出现，避免玩家一直被推去"修炼流水线" |
| `奇遇` | 偶发剧情钩子 | 金龙鱼、路遇门派弟子 | 长期占比目标 15%~25%，过高会显得刻意，过低显得死气 |
| `社交` | NPC 互动、因果推进 | 拜师、结怨、还愿 | 依赖 `CauseLink` 密度，因果多的角色理应更常触发 |
| `天劫` | `force` 来源专属 | 走火入魔、渡劫 | 不参与常规配额，由 `EventArbiter` 强制级单独处理 |
| `环境` | 天气、灵气异动叙述 | 潮汐提示、异象 | 装饰性，几乎不改状态，只输出叙述 |

### 3.3 内容密度设计原则（衔接新鲜感机制）

ARCHITECTURE 的 `NoveltyWeight` 只能让"已有的事件"轮着来，**填不满的内容池靠软件机制救不回来**。设计侧的硬性配额：

- 每个「地点类型 × 标签」组合，MVP 阶段至少 **6 条**不同 `event_id`，其中至少 2 条挂 3 条以上 `EventVariant`。低于这个密度，`AgentEventHistory` 的长尾保护会频繁"无长尾可保护"，抽取会明显变得可预测。
- 「奇遇」标签要求内容更分散：同一地点类型下的奇遇事件之间**结果类型要错开**（不能连续 6 条都是"捡到东西"），策划自查时按 `ResultPoolEntry.kind` 分布检查，物品/因果/连锁/检定四类都要有覆盖。
- 具体排期见 §9。

---

## 4. 万年历与天象系统内容规格

### 4.1 历法结构与展示格式

| 层级 | 单位 | 展示示例 | 备注 |
|---|---|---|---|
| 纪元 | `太乙历` | 太乙历 | MVP 只有一个纪元，为 V2 转世跨纪元预留 |
| 年 | 数字 + 干支 | 九四七六年 · 甲辰年 | 两种记法同时展示，干支年用于灵气潮汐等周期计算 |
| 月 | 1-12 | 三月 | |
| 日 | 1-30（简化，不做真实历法闰月） | 十五 | |
| 时辰 | 12 时辰名 | 午时 | 子/丑/寅/卯/辰/巳/午/未/申/酉/戌/亥 |

### 4.2 灵气潮汐与天劫的具体数值

| 现象 | 触发条件 | 效果 |
|---|---|---|
| 灵气潮汐 | 每月初一、十五 | 全地点灵气浓度 ×1.5；`meditate` 修为收益同比例提升；妖兽类事件权重 ×2（"狂暴期"） |
| 天劫窗口 | 境界达到「渡劫」且当年干支满足特定推算规则（策划配置，示意：与出生干支相冲之年） | 该窗口内 `force` 标签的天劫事件权重从 0 提升到非零，仍是概率事件，可能一直不命中直到寿元耗尽 |
| 灵气浓度基线 | 由 `Location.灵气浓度` 属性 + 潮汐乘子共同决定 | 秘境/宗门山门基线更高（如 0.8），闹市更低（如 0.3） |

### 4.3 闭关交互流程

```
玩家：「闭关」
系统：「要闭关多久？（可以说"十年""到金丹为止""随便"）」
玩家：「十年」
系统：「你闭下心来，运转周天……」
      ↓（RetreatService 同步批量结算，见 ARCHITECTURE §4.11）
系统：「十年过去，你的修为从 68/100 涨到了 筑基一层 12/100，寿元略有损耗。
       期间苍梧城经历了一次灵气潮汐，你恰好错过了。」
```

- 闭关时长支持模糊表达："到金丹为止"翻译成一个允许中途因 force 事件（走火入魔/天劫）打断的**目标条件**而非固定时长；"随便"由系统给一个默认建议值（如当前境界剩余修为估算的游戏年数）并二次确认。
- 结算摘要**必须**包含：修为/寿元变化、期间发生但被跳过的全局事件类型汇总（"错过了一次潮汐/两次门派招新"），不需要逐条罗列——呼应 README 1.6"时间留存语义"里"不强制对齐每一件事"的设计。

---

## 5. 地图与探索内容规格

### 5.1 MVP 示例世界

一份手编世界地图示例（供内容团队起步用，非最终稿）：

```
        [藏剑山门]
            |
[黑风谷]—[苍梧城]—[落雁镇]
            |
        [归墟秘境]（隐藏，需神识扫描发现）
```

| 地点 | 类型 | 灵气浓度 | 特色标签 |
|---|---|---|---|
| 苍梧城 | 城市 | 0.4 | 生活/社交密集，新手出生地 |
| 落雁镇 | 城市 | 0.3 | 集市为主，经济类事件密集 |
| 藏剑山门 | 宗门 | 0.7 | 拜师、修炼类事件 |
| 黑风谷 | 荒野 | 0.5 | 战斗/危险类事件，妖兽出没 |
| 归墟秘境 | 秘境 | 0.9 | 高境界限定，隐藏点位 |

### 5.2 城市内部模板

每座城市固定生成三类节点 + 按规模随机追加：

- **固定**：集市（购物/经济事件）、主街（生活/奇遇事件，出生点默认落这里）、城门（移动枢纽，无事件挂载）。
- **随机（1~3 个）**：酒楼、当铺、镖局、义庄……从模板池按城市规模抽取，决定该城市的"性格"（有酒楼的城市生活事件更丰富，有镖局的城市奇遇偏冒险向）。
- **建筑内部**：进入酒楼等节点后加载子图（同一 `location_id` 下的子地点，如"雅座""柜台"），子图节点复用父节点的地点类型做粗筛，除非单独配置。

### 5.3 探索反馈设计

- **神识扫描**：`inspect`类只读命令，不消耗回合、不进 `AgentEventHistory`；命中隐藏点位的概率由地点的"隐蔽度"属性决定（策划配置，示意 15%~40%），未命中给一句烘托叙述而非"无事发生"的冷淡回复（"你运转神识，一时并无异常"）。
- **地点状态可见提示**：`废墟`态地点在叙述开头固定插入环境描写钩子（"断壁残垣间尚有余烬味"），玩家能从叙述判断出"这里发生过事"，不需要额外 UI 标记。

---

## 6. 虚拟人物内容规格

### 6.1 NPC 生成规则

| 属性 | 生成方式 | 示例 |
|---|---|---|
| 灵根 | 五行随机（金木水火土，可复合） | "水木双灵根" |
| 资质 | 正态分布，均值对应"中人之姿" | 影响修炼速度倍率 0.5x~2.0x |
| 运势 | 均匀分布 | 影响奇遇标签权重加成 |
| 出身 | 按出生地城市模板抽（商贾/农家/散修/宗门弟子） | 决定初始 flags 与可用别名（商贾之后更容易触发买卖类事件） |

### 6.2 履历模板 vs 细履历

**模板一句话**（未展开细履历的 NPC，README 1.5.1）：

```
"王麻子，二十岁因资质平庸被逐出师门，如今在坊市摆摊算卦为生。"
```

模板由固定句式 + 随机填槽（年龄段、事件类型、职业）拼接，不经过事件库随机抽取。

**细履历**（同城/有因果/被查询过的 NPC，步数上限 8）：真实从事件库随机抽取关键事件，写入 `Biography` 事件流，查询时按时间顺序渲染成一段传记文本，**允许比模板更离谱**（因为走的是同一套 `GameEventDef` 随机接龙，不是策划特意写的人生剧本）。

### 6.3 查询 NPC 的信息卡

「打听王麻子」返回只读卡片：

```
┌ 王麻子 ─────────────┐
│ 出身：散修            │
│ 现状：坊市摆摊         │
│ 与你的关系：无因果记录  │
│ 生平：二十岁因资质平庸  │
│      被逐出师门…       │
└──────────────────────┘
```

有 `CauseLink` 时追加关系行（"仇人""恩人""师徒"），带过期倒计时的因果标注"（20年内有效）"。

---

## 7. 数值系统详细设计

对应 ARCHITECTURE `BalanceTable`，本节给策划可直接抄的初始配置。

### 7.1 境界表

| 境界 | 修为需求（示意） | 推荐游戏年数区间 | 基础寿元上限 |
|---|---|---|---|
| 凡人 | — | 0~15 岁 | 80 |
| 练气 1~9 层 | 每层 100 | 约 10 年（新手期） | 120 |
| 筑基 | 1500 | 约 20 年 | 200 |
| 金丹 | 4000 | 约 30 年 | 350 |
| 元婴 | 10000 | 约 60 年 | 600 |
| 化神 | 25000 | 约 100 年 | 1000 |
| 渡劫 | 50000 | 不定（天劫窗口触发） | 1500（若渡劫失败寿元归零） |
| 大乘 | 100000 | 长线 | 3000 |
| 仙人 | — | 终局 | 不老（叙事性终局，非数值上限） |

`BalanceTable.realm_order` 就是这张表第一列的顺序；`lifespan_by_realm` 对应第四列。

### 7.2 突破成功率算例

公式（README 2.4）：`P(突破) = clamp(资质×灵气浓度×丹药加成 − 心魔 − 境界惩罚, 0.05, 0.95)`

示例代入（练气三层 → 四层，资质 1.0 中人之姿，灵气浓度 0.4，无丹药，心魔 0.1，境界惩罚 0.05）：

```
P = clamp(1.0 × 0.4 × 1.0 − 0.1 − 0.05, 0.05, 0.95)
  = clamp(0.25, 0.05, 0.95) = 0.25
```

25% 成功率——**刻意偏低**，鼓励玩家先「打坐」攒灵气浓度更高的地点或嗑丹药，而不是无脑连续突破。失败后果：修为回退 20%，心魔 +0.05；连续 3 次失败心魔达阈值触发走火入魔（`force` 链）。

### 7.3 战斗检定算例

公式：`P(胜) = clamp(境界差 + 道具 + 运势 − 心魔, 0.05, 0.95)`，境界差按"每高一级 +0.15，每低一级 -0.15"换算。示例（同境界对战，带一件+0.1道具）：

```
P = clamp(0 + 0.1 + 0(运势基线) − 0.05, 0.05, 0.95) = 0.55
```

### 7.4 经济收支估算

| 类型 | 来源 | 典型数额 |
|---|---|---|
| 收入 | 奇遇掉落变卖、日常小奇遇打赏 | 每次奇遇 5~50 |
| 支出 | 吃饭 | 每次 2~5 |
| 支出 | 拜师礼 | 一次性 50~200 |
| 支出 | 丹药 | 每颗 20~100（效果越强越贵） |

新手期（练气阶段）预期"收支紧平衡略有盈余"，避免金钱压力盖过探索欲；金丹以后金钱数字意义弱化（叙事优先），不做后期数值膨胀式刷钱设计。

### 7.5 隐藏属性对玩法的影响（不做面板展示，仅供策划配置结果池时参考）

| 属性 | 影响 |
|---|---|
| 心魔 | 突破/战斗成功率负修正；达阈值触发走火入魔；日常叙述里插入侧面描写 |
| 悟性 | 影响顿悟类奇遇的触发权重（悟性高的 Agent，`novelty_curve_override` 可配更快的顿悟事件恢复） |
| 运势 | 影响奇遇标签的粗筛通过率（可实现为一个隐藏谓词 `luck_gte`，不在 README 白名单谓词里公开暴露给编辑器随意用，仅系统内置事件使用） |

---

## 8. 死亡与重玩内容规格

### 8.1 死亡文案模板

```
[系统] 寿元耗尽，你在苍梧城的老宅中安然离世。
       后人为你立下墓碑：
       「王小二，太乙历一百八十年卒，享年八十。
        一生历经三次突破，两位知己，一段未竟的仇。」
```

碑文由 `Biography` 关键事件流自动摘要（取因果标签最重的 2~3 条 + 最高境界 + 享年），不需要人工撰写模板内容，只需策划定摘要规则的优先级。

### 8.2 三选一交互

```
你的旅程结束了。接下来——
  转世：以凡人之身重新开始，带走部分资质与悟性，仇人可能寻上门。
  夺舍（需金丹以上）：残魂夺取他人躯壳，境界降一级，心魔大增。
  继承：让一位后辈接过衣钵，继承部分资源。

（打字说出你的选择，比如"转世"）
```

- 保持"打字输入"而非按钮，与全局交互基调一致；未达条件的选项在文案里说明原因而非隐藏（"夺舍：境界不足，暂不可选"），保留信息透明度。

### 8.3 三条路径的体验设计意图

| 路径 | 玩家会选它的理由 | 设计钩子 |
| --- | --- | --- |
| 转世 | 想要一个"干净"的重新开始，但带着上一世的因果余韵 | 继承的 `CauseLink` 带来"熟悉的陌生感"——同一世界，故人已非旧貌 |
| 夺舍 | 舍不得放弃已有境界，愿意承担代价 | 心魔大增制造后续玩法张力（更容易走火入魔），是"高风险续命" |
| 继承 | 想体验"家族/门派"的长线叙事 | 后代继承因果关系网，仇人/恩人关系代际传递，适合喜欢关系网玩法的玩家 |

---

## 9. 内容生产计划

衔接 ARCHITECTURE.md §11 TODO 与 README 1.4.3 新鲜感机制——机制只能让"有限内容"显得耐玩，填不满的池子机制救不了。

### 9.1 MVP 最小内容量估算

按 §3.3 的密度原则（每「地点类型×标签」≥6 条），MVP 世界（§5.1 五个地点、约 4 种地点类型）估算：

| 地点类型 | 标签数（生活/修炼/奇遇/社交） | 每标签条数 | 小计 |
|---|---|---|---|
| 城市 | 4 | 6 | 24 |
| 宗门 | 3（修炼/社交/奇遇） | 6 | 18 |
| 荒野 | 3（奇遇/社交/环境） | 6 | 18 |
| 秘境 | 2（奇遇/环境） | 6 | 12 |
| **通用**（`applicable_locations=["*"]`，全地点可用） | 生活基础事件（吃饭/打坐等） | ~10 | 10 |

**MVP 总量目标：约 80~90 条 `GameEventDef`**，含流程图的（如金龙鱼多轮互动）按一条主事件+挂接的 `ReplyOption`/`ScenarioGraph` 计，不重复计数分支。低于这个量，`NoveltyWeight` 的长尾保护机制会经常"无牌可发"。

### 9.2 排期优先级

1. **P0（决定"能不能玩"）**：`move`/`eat`/`meditate`/`breakthrough` 等命令型事件全套 + 每个地点至少 3 条奇遇兜底，避免玩家第一局就撞上"抽空"的空气感。
2. **P1（决定"耐不耐玩"）**：补齐 §9.1 密度表到位，含文案变体（每条高频事件 ≥3 条 `EventVariant`）。
3. **P2（决定"有没有深度"）**：社交/因果链事件、NPC 细履历触发条件、死亡后三选一路径的差异化内容。

### 9.3 录入自检清单（呼应 ARCHITECTURE §4.12 校验）

策划提交一条事件前自查：

- [ ] 谓词是否只用白名单类型，能否用编辑器"模拟触发"跑出预期合格池
- [ ] 是否有至少 1 条默认 `EventVariant`，高频事件是否配够 3 条变体
- [ ] 结果池是否只用类型化 `Result`，没有在文案里暗示"给了什么但没配到结果池"（叙述与数值必须对得上，否则玩家会觉得系统在骗人）
- [ ] `cooldown_shichen`/`max_trigger_per_agent`/`exclusive_tags` 是否按 §3.3 密度原则配置，避免同类事件互相挤占
- [ ] 是否需要 `needs_reply`（`reply_options`/`scenario_ref`），若是，局部选项的别名是否覆盖了"接受/拒绝/无关的话"三种玩家反应

---

## 10. 与其它文档的对接索引

| 本文档章节 | README 对应节 | ARCHITECTURE.md 对应结构 |
|---|---|---|
| §2 界面设计 | 2.3.1 万年历 UI | `view/calendar_view.py`、`TurnResult.prompt_event_id` |
| §3.1 命令语言 | 1.11 文字命令 | `ChatParser.parse` / `parse_reply` |
| §3.3 内容密度 | 1.4.3 新鲜感与去重机制 | `AgentEventHistory`、`novelty_weight` |
| §4 万年历天象 | 1.2.1 / 2.3.1 | `GameClock`、`clock_service.advance_for` |
| §5 地图探索 | 1.2.2 / 2.3.2 | `Location`/`Route`、`WorldDiff` |
| §6 虚拟人物 | 1.5 / 2.3.3 | `Biography`、`biography_service` |
| §7 数值系统 | 2.4 | `BalanceTable`、`Check` 结果类型 |
| §8 死亡重玩 | 2.5 | `death_service`（ARCHITECTURE §4.13） |
| §9 内容生产 | 1.3.2 / 1.3.3 | `event_validation.py`（ARCHITECTURE §4.12） |
