这是一个非常经典的问题：如何将不受约束的自然语言，映射到受限的游戏系统状态中。
在您的修仙游戏中，直接告诉玩家“听不懂”是破坏沉浸感的大忌。处理非预期输入的核心原则是：不要拒绝，要“理解并转化”，即使转化结果是“无意义的消耗”。
针对您的框架，我建议采用 “三层兜底策略”，从简单语义匹配到 LLM 智能理解，层层递进：
策略一：基于语义的指令模糊匹配（利用现有的 BGE 模型）
既然您已经有了 bge-small-zh 向量模型，不要只把它用于“事件生成”，也可以用于 “指令解析”。
原理：
建立一个小型的“指令向量库”，包含所有合法的游戏动作（如 吃饭、去酒楼、打坐、修炼、查看属性）。当用户输入任意文本时，计算其与这些指令的相似度。
处理流程：
用户输入："到处走走看"
向量化该输入。
计算与指令库的余弦相似度：
去酒楼 (相似度: 0.65)
修炼 (相似度: 0.20)
打坐 (相似度: 0.22)
逻辑判断：
如果最高分 > 0.8：直接执行该指令。
如果最高分在 0.5 - 0.8 之间：意图确认。
*输出*：“道友是想去【酒楼】看看吗？（y/n）”
如果最高分 < 0.5：进入策略二或三。
针对 到处走走看 的优化：
您可以预置一个通用指令 探索 或 闲逛。
"到处走走看" 与 探索 的相似度通常很高。
系统响应：触发 ExploreEvent，结果生成一段随机文本：“你在苍梧城的街道上漫无目的地闲逛了一阵，只看到了熙熙攘攘的人群，并未发现什么机缘。”
策略二：LLM 意图结构化提取（最推荐，最灵活）
这是现代 AI 游戏的主流做法。利用 LLM 强大的理解能力，将“乱七八糟”的输入强制转化为 JSON 格式的游戏动作。
Prompt 设计：
你是一个文字修仙游戏的指令解析器。请将玩家的自然语言输入解析为游戏动作。
如果玩家输入无法匹配任何具体动作，请将其归类为 "idle"（发呆/闲逛），并提取他想描述的场景关键词。
当前地点：苍梧城
周围环境：酒楼、街道、行人
玩家输入："到处走走看"
请以 JSON 格式输出，不要包含其他内容：
{
  "action_type": "move" | "interact" | "rest" | "idle",
  "target": "目标对象或地点",
  "confidence": 0.0-1.0,
  "response_text": "如果无法理解，生成的安抚性旁白"
}
预期输出：
{
  "action_type": "idle",
  "target": "街道",
  "confidence": 0.6,
  "response_text": "你在街头徘徊了一会儿，并未确定去向。"
}
代码逻辑：
调用 LLM 获取 JSON。
如果 action_type 是 move、interact 等已知类型，且 confidence 高，则执行对应逻辑。
如果是 idle 或者 confidence 低，则直接将 response_text 输出给玩家，并消耗少量的游戏时间（如半个时辰）。这样玩家感觉自己说了话，且产生了后果（时间流逝），而不会感到被系统“拒绝”了。
策略三：通用动作兜底
如果向量匹配分低，LLM 调用失败（或不想频繁调用 LLM），则启用 “通用描述动作”。
在修仙游戏中，所有的行为都可以归纳为：修、行、战、聊、杂。
映射规则：
输入包含动词“走、跑、逛、游” -> 映射为：闲逛
*反馈*：“你四处游荡了一番……”
输入包含疑问词“什么、怎么、哪里” -> 映射为：自省/观察
反馈：“你观察了一下四周……” 或 调用 HelpCommand。
输入包含负面情绪“烦、不想” -> 映射为：静心
*反馈*：“你深吸一口气，平复了心绪。”
针对您的示例：
输入：> 到处走走看
匹配到：闲逛
系统执行：
发布 TimePassEvent（时间 +10分钟）。
发布 RandomTriggerRollEvent（由于是在闲逛，给予极低概率触发小彩蛋，比如捡到一枚铜钱）。
输出：“你在苍梧城的大街小巷穿行了一阵，虽无大机缘，却也对这红尘俗世多了几分了解。”
综合推荐方案
结合您的 EventHorizon 底座，推荐实现一个 NaturalLanguageParser (自然语言解析器) 组件：
第一步（关键词过滤）：检查是否包含直接指令（如“查看”、“背包”），如果有，直接执行。
第二步（向量语义匹配）：使用 BGE 计算与 吃饭、打坐、探索 的距离。
如果是 探索，直接触发探索逻辑。
第三步（LLM 兜底）：如果前两步都无法确定，发送给 LLM。
让 LLM 决定是执行某个动作，还是仅仅生成一段描述性文本。
第四步（终极兜底）：如果一切失败，绝不报错。
输出：“你心念一动，却不知该作何抉择，只得在原地伫立片刻。”（消耗少量时间）。
伪代码示例：
async def handle_player_input(raw_input: str):
    # 1. 尝试向量匹配已知指令
    match = semantic_match(raw_input, known_commands)
    if match.score > 0.75:
        return execute_command(match.command)
    # 2. 尝试 LLM 解析
    try:
        intent = await llm_parse_intent(raw_input)
        if intent.action_type != "unknown":
            return execute_intent(intent)
        else:
            # LLM 认为不知道，但给了一段安抚文本
            return print_text(intent.response_text + " (时间流逝)")
    except:
        pass
    # 3. 终极兜底：通用闲逛
    return execute_idle_action(raw_input)
通过这种方式，玩家的任何输入——哪怕是”啊啊啊”、”不知道”或”到处走走看”——都能得到游戏世界的合理反馈，极大地提升了 CLI 文字游戏的沉浸感。

## 实现情况补充（2026-09-13）

这份文档提出的”三层 + 终极兜底”思路已经在 EventHorizon 里落地，比最初设想的更细一层。完整记录见 README 第 1.13～1.16 节，这里只记落地映射，不重复贴代码：

**策略一/二/三 + 终极兜底 → 四层兜底链（README §1.13，`PlayTurnService.handle_player_text`）**：
1. `ChatParser.parse` 精确/子串别名匹配。
2. 向量意图兜底（`matching.py::find_best_matching_command`），对应策略一”语义模糊匹配”。
3. `LiveContentAuthor` 实时创作（README §1.12 的对局隔离例外），对应策略二”LLM 结构化提取”——产出的是真实的 `GameEventDef`，立即落库生效，不是本文档设想的一次性 JSON。
4. **终极兜底**：预置的 `idle_wander` 命令型事件（`content/events/commands.py`），第 4 轮起兜住前三层全部落空的情况，绝不显示”听不懂”——直接执行这条事件，给一段”什么都没发生但话被听懂了”的旁白并消耗一点时间，跟本文档”终极兜底：`execute_idle_action`”的思路一致，只是实现成了一条真正的、带别名的库内事件（不是硬编码文案），顺带能被第 2 层向量兜底自然命中，不需要单独的关键词特判。

**在原策略之外，这次连带做的扩展**：
- **世界信息问答**（README §1.14）：`WorldQueryAssistant` + `game_context_tools.py`，用真正的 function calling 回答”我在哪里””附近有哪些城市”这类元问题——这类问题走一条独立的只读路径，不进上面那条命令兜底链，不消耗回合。
- **实时创作事件的延迟结果**（README §1.15）：一个动作可以同时有立即生效和”故事线收尾后才生效”两部分结果（比如”帮了忙得了赏钱，但也可能得罪了对头，日后要找麻烦”）。
- **实时创作事件的分支结果**（README §1.16）：一个场景可以是”看到什么东西，然后看你怎么选”（比如本文档最初举的例子），复用库内已有的 `needs_reply`/`reply_options` 机制，不是另起一套。

> 针对整体架构和核心代码的审查结论（含一个会影响本文这条兜底链的确认 bug：移动意图正则会吞掉"去吃点东西"这类正常命令，导致第 2～4 层兜底根本没机会跑），见 [优化建议.md](优化建议.md)。

**实测跟原文档设想不完全一致的地方**：本文档假设”LLM 会老实告诉你它不确定/该给玩家一个选择”，但实测这个项目配的模型（glm-4-flash）并不太会这样——它几乎总是直接编一个完整、确定的场景，很少主动说”信息不够需要追问”，也很少主动用上面说的”分支”机制让玩家选，倾向于替玩家把决定做了。这不是实现的问题，是这个具体模型的行为倾向；兜底链的设计因此更偏向”退而求其次给默认值/把数值 clamp 到安全范围”，而不是依赖模型自己诚实地说”我不确定”。细节见 README §1.12/§1.15/§1.16 里各自的”已知模型局限”说明，换更强的模型时值得重新评估。