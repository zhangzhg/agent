# EventHorizon 代码与架构审查（2026-09-13）

> ## 实施状态（2026-09-13 同日更新）
>
> | 条目 | 状态 | 备注 |
> |---|---|---|
> | P0-1 移动正则吞命令 | ✅ 已修 | 别名表提到移动之前 + 目的地合理性校验；**顺带发现并修了第二个同类 bug**（见下） |
> | P0-2 快照无限增长 | ✅ 已修 | 保留最近 20 份 + 世界脏判定；1440 轮实测 **~340MB → ~2.2MB** |
> | P1-2 挂起态样板 | ⚠️ 部分 | 已消除 codec 侧的手抄样板（真正会导致"读档静默丢档"的那部分）；`Agent` 字段结构未动，见下方说明 |
> | P1-5 校验目录全量反序列化 | ✅ 已修 | 新增 `published_event_ids()`，不再为拿一把 id 反序列化整个事件库 |
> | P2 冗余局部 import | ✅ 已修 | `play_turn.py` 三处 |
> | P2 ARCHITECTURE.md 脱节 | ✅ 已修 | 顶部加了醒目的"原始设计存档"声明 + 已知脱节清单 |
> | P2 README 缺 `pending_clarification` | ✅ 已补 | §1.12 |
> | P1-1 拆分 `PlayTurnService` | ⬜ 未做 | 大重构，建议单独一轮 |
> | P1-3 结算逻辑重复/时间语义不一致 | ⬜ 未做 | **需要产品决策**，见下方说明 |
> | P1-4 LLM 预算上限 | ⬜ 未做 | 依赖 P1-1，见下方说明 |
> | P1-5 `live_` 事件回收 | ⬜ 未做 | 只做了上面那半 |
>
> 测试从 418 增至 **434 全绿**。
>
> **P0-1 顺带挖出的第二个 bug**：为它加的"所有注册别名必须可达"回归测试，立刻
> 又抓出两个死别名——`IDLE_WANDER` 的 `走走看看`/`四处看看` 被 `WATCH` 更短的
> `看看` 永久遮死（别名匹配取的是"第一个命中"，而 dict 按插入顺序遍历，先注册的
> WATCH 赢）。已改成**最长别名优先**，这是别名匹配本该有的语义，也一次性堵住了
> 整类子串遮蔽问题。这条正好印证了写那个测试的价值：它抓到的不止是当初那一个。
>
> **P1-2 为什么只做一半**：原方案建议把 `Agent` 上 5 个挂起态字段收进
> `pending: dict[str, PendingState]`。真动手评估后，这会波及 `states.py::settle()`、
> 各 controller、以及大量测试里的 `agent.pending_scenario` 直接访问，改动面很大而
> 收益主要是"少写样板"。而**真正的风险**不在样板多，在于"漏抄一处 → 读档静默丢
> 状态、没有任何报错"，那个风险集中在 codec 的六处手抄。所以这次只把 codec 改成
> 按 `dataclasses.fields` 自动展开（`codec.py` 594→553 行，`__unset__` 样板 15→8
> 处），并加了一条**遍历 dataclass 全部字段**的往返测试盯着它。字段结构的大重构
> 留给 P1-1 一起做更划算。
>
> **P1-3 为什么没动**：`_resolve_reply_option` / `_advance_scenario` 不推进游戏时间，
> 意味着"选一个分支""走完一整条流程图"都是零时间成本。这是**游戏平衡决策，不是
> 纯技术问题**——该不该让分支消耗时间、消耗多少，应该由你定，我不该单方面改掉。
> 另外真去抽公共函数时发现四处的差异（要不要发 `AgentStateChanged`、要不要记
> 事件历史、连锁处理）比共同点还多，硬抽会得到一个四个布尔开关的函数，比现在的
> 重复更难读。建议：先定时间语义，再连同 P1-1 一起动。
>
> **P1-4 为什么没动**：加"每回合 LLM 调用预算"需要把预算对象贯穿到所有可能调
> 模型的地方。当前这些调用点全散在 `PlayTurnService` 里，而该类文件头明确写着
> "服务对象一律无跨回合状态"——把计数器挂在 `self` 上会直接违反这条不变量。干净
> 的做法是随 P1-1 拆分时把预算作为参数传下去，所以排在 P1-1 之后。


一次针对现有代码和文档的整体 review，按"改了收益最大"排序。每条都标注了证据（可复现的验证方式或具体文件行号），以及"改/不改"的判断依据。

结论先行：

- **架构底子是好的**，分层、diff 唯一写入口、快照+重放边界、fail-open 的向量降级这些核心约束都立得住，而且有测试守着（`tests/test_layering.py`）。不需要推倒重来。
- **真正该动的是两处**：一个确认的解析 bug（P0-1，正在静默吞掉玩家指令），和 `PlayTurnService` 的体量失控（P1-1）。
- **文档的问题比代码大**：`ARCHITECTURE.md`（1419 行）已经和实现严重脱节，继续放着会主动误导人。

---

## P0：确认的 bug，建议尽快修

### P0-1 移动意图正则吞掉正常命令（含内置别名）

`chat_parser.py:39` 的 `_MOVE_PATTERN_UNANCHORED = re.compile(r"(?:去|前往)\s*(.+)$")` 是全文搜索，而 `parse()` 在 `chat_parser.py:92` 把 `_match_move` 放在**别名匹配之前**。"去"在中文里大量作为动词前缀出现（去做、去看、出去、回去），结果是任何含"去"的句子都被判成移动。

实测（10 句里 8 句误判）：

| 输入 | 实际解析 | 应该是 |
|---|---|---|
| `去吃点东西` | move，目的地 `吃点东西` | **`eat`——这正是 `commands.py:25` 注册的 EAT 别名** |
| `我想去打坐` | move，目的地 `打坐` | `meditate` |
| `出去到处走走` | move，目的地 `到处走走` | `idle_wander`（别名就是"到处走走"） |
| `我打算去修炼一下` | move，目的地 `修炼一下` | `meditate`（别名"修炼"） |
| `我不想去了` | move，目的地 `了` | 无 / 兜底 |
| `他走过去了` | move，目的地 `了` | 无 / 兜底 |

复现：

```bash
cd eventhorizon && python -c "
from model.services.chat_parser import ChatParser
p = ChatParser({'吃饭':'eat','去吃点东西':'eat','打坐':'meditate','修炼':'meditate','到处走走':'idle_wander'})
for t in ['去吃点东西','我想去打坐','出去到处走走','我打算去修炼一下','我不想去了']:
    print(repr(t), '->', p.parse(t).event_id)
"
```

**为什么这条排 P0**：

1. `content/events/commands.py:25` 精心写的别名 `"去吃点东西"` 是**彻底不可达的死代码**——注册了但永远匹配不上。
2. 它**preempt 了整条四层兜底链**（README §1.13）。第 2 层向量兜底、第 3 层实时创作、第 4 层 idle_wander 全都没机会跑，因为第 1 层就误判成移动了。上一轮做的 idle_wander 终极兜底，对所有含"去"的句子直接失效。
3. **会污染世界数据**：误判成 move 后找不到"修炼一下"这个地点，`_handle_move` 就调 `_author_live_destination_outcome` 去实时创作地点——一个叫"了"或"修炼一下"的 `Location` 可能被真的建出来并写进快照。这是从解析 bug 传导到持久化数据的污染。

**建议改法**（按推荐度）：

1. **调换顺序 + 让移动成为兜底解释**：先跑别名表，命中就返回；只有别名没命中才尝试移动解析。改动最小、语义最直观，也符合"移动只是众多命令之一"的定位。
2. 若要保留移动优先，至少加一道否决：`去` 后面的剩余文本若命中任何已注册命令别名，就不当移动。
3. 顺带收紧 `回` 的处理——`回去吧` 现在被解析成"去『去吧』"。

补一条回归测试：**内容库里注册的每一个命令别名，`ChatParser.parse(alias)` 都必须解析回它自己的 `event_id`**。这类"注册了却不可达"的问题以后就能自动发现。

### P0-2 快照无限增长，且每回合写两份全量

`chat_controller.py` 每回合无条件调用 `agent_repo.save()` + `world_repo.save()`，两者各自 `INSERT` 一行**全量** JSON 快照（`snapshot_store.py:33`，是 INSERT 不是 UPSERT），全库没有任何清理逻辑（grep `DELETE FROM snapshots` / `prune` 均无结果）。

实测：单个 world payload 约 4KB、agent 约 0.9KB，合计**每条玩家消息约 10KB，永久累积**。1000 轮对话 ≈ 10MB，且 `LiveContentAuthor` 每创作一个地点，world payload 就再变大一点，是复利增长。

**建议**：

1. `world_repo.save()` 加脏标记——世界绝大多数回合根本没变，这一半的写入是纯浪费。
2. 快照只保留最近 N 份（或按时间窗口），后台清理旧行。注意 `load()` 依赖 `replay_since(快照时刻)`，裁剪时必须保证"最老的保留快照"之后的日志都还在，不能只删快照不管日志。
3. 若确认单机单存档不需要历史快照，最省事的是改成"单行 upsert + 事件日志"，快照表恒定 1 行。

> 顺带确认一个**没有**问题的点：`replay_since` 用的是严格大于（`event_log.py:48`），快照时刻那一条不会被重放两遍。这个边界处理是对的，注释也写清了理由，别在裁剪时改坏它。

---

## P1：架构层面，建议排期做

### P1-1 `PlayTurnService` 已经是 God Class（852 行）

它是全项目最大的文件（第二名 `codec.py` 594 行），现在一个类同时负责：

- 输入路由与四层兜底链
- 5 种挂起态的判定、推进、放弃
- 实时创作（命令 / 地点 / 追问 / 延迟结果 / 分支）的编排
- 系统命令特判（move / retreat）
- 两段式结算、第二段抽取
- 流程图推进、选项结算

最直接的证据是 `handle_player_text` 的分支编号已经变成 `-1) 0) 1) 1.5) 2)`——**出现负数和小数编号，说明这里是反复往中间插逻辑插出来的，不是设计出来的**。另有 15 处 `apply_agent_diff` 直接散在各方法里。

**建议拆分**（保持 `PlayTurnService` 作为对外门面不变，避免动 bootstrap 接线和一大票测试）：

- `TurnRouter` / `PendingResolver`——负责 `handle_player_text` 那条优先级链和所有挂起态的推进。
- `LiveAuthoringCoordinator`——`_author_live_*` / `_resolve_*_outcome` / `_maybe_conclude_live_result` 这一组，现在占了近 200 行。
- `OccurrenceExecutor`——`execute_occurrence` / `_second_stage` 这条真正的两段式流水线。

这样 `play_turn.py` 回到 200 行左右的编排层，每个协作者都能单独测。

### P1-2 挂起态没有抽象，加一个要改 4 个文件约 10 处

`Agent` 上现在有 5 个挂起态（`agent.py:229,230,242,243,244`）。这一轮亲手加了两个，每次的固定成本是：

1. `domain/agent.py`：dataclass + `Agent` 字段
2. `domain/diff.py`：`AppliedDiff` 字段 + `merge` 分支 + `apply_agent_diff` 分支
3. `repositories/codec.py`：`applied_diff_to/from_dict` + `agent_to/from_dict` 四处
4. `services/play_turn.py`：`handle_player_text` 里的优先级分支 + set/clear 辅助方法

`codec.py` 里 `__unset__` 哨兵这套样板出现了 **15 次**，`diff.py` 里 4 次。每加一个挂起态就多抄一遍，而且**漏抄任何一处的后果是"读档静默丢状态"**——`codec.py` 顶部注释自己写着"读档丢一个都会露馅"，但结构上完全依赖人不忘记。

**建议**：引入一个挂起态注册表。让每种挂起态实现一个统一协议（`serialize` / `deserialize` / `priority` / `try_resolve`），`Agent` 上换成 `pending: dict[str, PendingState]`，codec 走通用循环，`handle_player_text` 按 `priority` 遍历。加一个新挂起态就变成"新增一个类并注册"，样板归零，也顺带把 P1-1 里那条 `-1) 0) 1) 1.5)` 的手写优先级链变成有序数据。

这是本次 review 里**结构性收益最大**的一条：它同时解决了"样板多"和"容易漏序列化导致丢档"两个问题。

### P1-3 结算逻辑四处重复，且时间语义不一致

"跑 pipeline → 推进时间 → 记事件历史 → settle → 投递 spawned"这套在四个地方各写了一遍，而且**只有两处推进时间**：

| 位置 | pipeline | `clock.advance_for` | `event_history.record` |
|---|---|---|---|
| `execute_occurrence` (`play_turn.py:590`) | ✓ | ✓ | ✓ |
| `_second_stage` (`:655`) | ✓ | ✓ | ✓ |
| `_resolve_reply_option` (`:703`) | ✓ | **✗** | **✗** |
| `_advance_scenario` (`:752`) | ✓ | **✗** | **✗** |

后两者不推进时间是既有行为（上一轮做分支功能时特意跟它对齐了，避免两套 needs_reply 事件语义打架），但站在游戏设计角度这是个洞：**玩家选"买下人参"、走完一整条流程图分支，游戏内时间纹丝不动**，冷却和 `max_trigger_per_agent` 也不记账。分支越多的交互越"免费"。

**建议**：抽一个 `_run_and_settle(occ, defn, *, advance_time: bool)`，四处共用；然后把"选项/流程图节点要不要消耗时间"变成一个**显式的、有意识的决定**（我的建议是让 `ReplyOption` 和流程图节点各自带一个可选 `duration_shichen`，默认 0 保持现有行为，需要时能配），而不是像现在这样，是"分散在四处、谁也没明说"的隐式行为。

### P1-4 同步 LLM 调用没有预算上限

单条玩家消息最坏情况下会串行发起多次 LLM 往返：收尾判断（`check_storyline_concluded`）→ 世界问答工具循环（最多 3 轮）→ 实时创作 → `result_pool` 补问 → `_ensure_variants` 补文案。全部同步、全部阻塞、各自独立 try/except，**没有任何"这一回合总共最多花多少次调用 / 多少秒"的约束**。

单机自己玩问题不大，但一旦某次 GLM 变慢，玩家就是干等，而且看不出卡在哪。

**建议**：加一个每回合的 LLM 预算对象（最大调用次数 + 总超时），透传给这一轮所有可能调模型的地方；超预算就直接走已有的降级路径（这些路径都已经写好了，`reject`/默认值/兜底文案一应俱全，接上去成本很低）。顺带把每次调用的耗时打进日志，方便定位。

### P1-5 `live_` 事件只增不减，会污染事件库

每一次未命中的玩家输入都会创建一条**永久**的 `live_xxxxx` 事件（`play_turn.py:352`），`aliases` 里还塞了玩家原话。没有任何回收机制。后果：

- 录入编辑器的事件列表会被玩家的碎碎念淹没，管理员很难再从中找到手工内容。
- `_match_command_by_intent` 每回合装载的命令池只增不减，且每条新事件都要算一次 `narrative_embedding`。
- `_resolve_command_outcome` 每次创作都要 `load_event_defs(None)`——**全量**装载所有事件只为拿一个 id 集合做校验。

**建议**：给实时创作的事件打一个来源标记（`source="live"`），编辑器默认折叠/过滤；再加一条清理策略（比如"N 天未再次命中就删"，或按数量上限 LRU）。校验用的 id 集合改成仓库直接给 `all_ids()`，别再全量反序列化事件对象。

---

## P2：可以顺手做的清理

- **`ARCHITECTURE.md` 严重脱节**（见下一节，其实这条的实际优先级高于本组其它项，只是性质是文档不是代码）。
- **`play_turn.py:767 / 772 / 777` 三处冗余局部 import**：`from model.domain.diff import AppliedDiff, apply_agent_diff` 在模块顶部 `:19` 已经导入过了，这三处纯属噪音，直接删。
- **三元联合返回类型**：`_resolve_command_outcome` 返回 `ParsedCommand | TurnResult | None`，`_resolve_location_outcome` 返回 `Location | TurnResult | None`，逼得每个调用点都写 `isinstance` 分支。建议换成一个显式的小结果对象（`Resolved(kind, value)`），可读性和类型检查都会好很多。
- **`_looks_like_gibberish` 只认 CJK**（`play_turn.py:77`）：纯英文输入（哪怕是 `meditate` 这种有意义的词）会被判为乱码直接 reject。当前是中文游戏，影响有限，但值得知道这个边界。
- **`load_event_defs` 每回合被反复全量调用**（`_match_command_by_intent`、`_soft_guidance_message`、`_second_stage`、校验目录各一次）。校验目录那次已经改成 `published_event_ids()` 了，其余三次建议加一个回合级缓存，或者仓库层做个简单的内存索引。

- **`GameTime` 的序列化很占地方**（修 P0-2 时实测发现的新问题）：每个时刻都存成
  `{"epoch":"太乙历","year":100,"month":1,"day":1,"shichen":6}`，约 70 字节。
  `AgentEventHistory.recent_tags` 存 200 个、`triggers` 每个事件存 64 个，于是单个
  Agent 快照收敛到 **~120KB**，其中 `event_history` 占 24KB 以上、光 `recent_tags`
  就 15KB。这些**都是有上界的**（`MAX_TRIGGERS_PER_EVENT=64`、
  `TAG_QUOTA_WINDOW*4=200`，原作者已经做了截断，不是泄漏），所以不紧急；但如果
  以后想再压一档，把 `GameTime` 存成一个整数 ordinal（`_ordinal()` 已经有了，
  反解也是纯算术）能直接省掉绝大部分体积。属于"要动持久化格式"的改动，需要一次
  存档迁移，建议攒着和别的格式变更一起做。

---

## 文档层面

### ARCHITECTURE.md 已经不能作为参考了，建议明确降级

这份 1419 行的"详细设计文档"里，本轮及此前新增的子系统**一个都没有**：

| 子系统 | ARCHITECTURE.md | README.md |
|---|---|---|
| `LiveContentAuthor` | 0 处 | 9 处 |
| `deferred_result_pool` | 0 处 | 5 处 |
| `idle_wander` | 0 处 | 3 处 |
| `llm_tool_loop` | 0 处 | 2 处 |
| `pending_live_result` | 0 处 | 2 处 |
| `WorldQueryAssistant` / `game_context_tools` | 0 处 | 各 1 处 |
| `pending_clarification` | 0 处 | 0 处（**两边都没有**） |

更麻烦的是它内嵌了大量**代码清单**，而代码清单是必然腐烂的。比如 §3.3.3 的 `Agent` 列了 3 个可选字段，实际有 12 个；`PendingScenario` 在列，`PendingClarification`/`PendingLiveResult` 不在。有人照着它写代码，会直接写错。

**建议二选一**：

1. **降级为设计存档**（推荐，成本最低）：在文件头加一段醒目声明——"本文是实现前的原始设计，代码清单仅存历史，当前实现以 README 第一部分 + 源码为准"，并列出已知偏离的清单。
2. **删掉所有代码清单，只保留不变量和取舍理由**。ARCHITECTURE.md 真正有长期价值的是"为什么这么设计"（为什么 diff 是唯一写入口、为什么状态切换必须经状态对象、为什么谓词用白名单不用 eval），这些不会腐烂；而 dataclass 字段列表属于源码的工作，不该在文档里抄一份。

另外 `pending_clarification` 两边都没写，README §1.12 只提到"最多追问 3 次"，没说明它是一个会进快照的挂起态——补一句。

### README 第一部分建议收拢

§1.12 ~ §1.16 现在是五个平级小节，讲的其实都是"`LiveContentAuthor` 的某个侧面"（例外边界 / 兜底链 / 世界问答 / 延迟结果 / 分支结果）。建议合并成一节带子节，读者能一眼看出它们同属一个子系统，而不是五个独立特性。

---

## 明确不建议改的地方

review 的一半价值是说清楚什么别动：

- **diff 作为唯一状态写入口**——这个约束很好地支撑了"实时对局与读档重放共用同一份 apply"，别为了图省事在别处直接 `setattr`。
- **`replay_since` 的严格大于边界**（`event_log.py:48`）——注释里记着它修过一个真实的重复应用 bug，动它之前先读注释。
- **`embed_safely` 的 fail-open 降级**（`matching.py:31`）——向量出问题时退化成中性权重而不是崩掉回合，这个取舍是对的。
- **`tests/test_layering.py` 的三条架构守卫**——尤其是 `LlmAuthorPort` 不得进入对局路径那条。`LiveContentAuthor` 是有意开的旁路、命名和职责都单独区分过，不要把它当成"这条守卫已经名存实亡"的理由去放宽它。
- **对 glm-4-flash 的各种"退而求其次"处理**（漏填 `result_pool` 给默认值、数值 clamp、示例占位符防护）——这些不是过度防御，每一条背后都有实测踩过的坑，README 各节都记了证据。换更强的模型时可以重新评估，但别在换模型之前先把防护拆了。

---

## 建议的实施顺序

1. **P0-1 移动正则**——影响最大、改动最小，且现在每天都在静默吞指令。连带补"所有注册别名必须可达"的回归测试。
2. **P0-2 快照增长**——先加 world 脏标记（几行），裁剪策略可以稍后。
3. **P2 文档降级声明**——十分钟的事，但能立刻止住"照着过期文档写代码"的损失。
4. **P1-2 挂起态注册表**——结构性收益最大，且做完之后 P1-1 的拆分会容易很多。
5. **P1-1 拆分 `PlayTurnService`** / **P1-3 结算逻辑归一**——一起做，互相配合。
6. **P1-4 LLM 预算** / **P1-5 `live_` 事件回收**——运营性问题，等实际玩起来数据变多了更有针对性。
